services:
  bot:
    build: .
    restart: always
    volumes:
      - ./files:/app/files
      - ./keys/id_ed25519:/root/.ssh/id_ed25519
      - ./keys/id_ed25519.pub:/root/.ssh/id_ed25519.pub
    env_file: "secrets.env"
    logging:
      driver: "json-file"
      options:
        max-size: "10M"
        max-file: "2"